{
  "exposure_notifications": {
    "schemas": {
      "CONFIG_SCHEMA": {
        "schema": {
          "config_vars": {
            "on_exposure_notification": {
              "key": "Required",
              "schema": {
                "config_vars": {
                  "esp32_ble_id": {
                    "key": "GeneratedID",
                    "type": "use_id",
                    "use_id_type": "esp32_ble_tracker::ESP32BLETracker"
                  },
                  "then": {
                    "type": "trigger"
                  }
                }
              },
              "type": "trigger",
              "docs": "**[Automation](https://next.esphome.io/automations)**: An automation to run when an exposure notification bluetooth message is received. A variable `x` of type <APIStruct text=\"exposure_notifications::ExposureNotification\" path=\"exposure_notifications::ExposureNotification\" /> is passed to the automation.\n\n*See also: [Exposure Notification Listener](https://next.esphome.io/components/exposure_notifications#configuration-variables)*"
            }
          }
        },
        "type": "schema"
      }
    }
  }
}