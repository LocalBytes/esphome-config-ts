{
  "name": "esphome-config-ts",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "build:generator": "node --loader ts-node/esm ./lib/build.ts",
    "build:tsc": "tsc",
    "build": "npm run build:generator && npm run build:tsc",
    "test": "node --loader ts-node/esm ./tests/esp8266_blink.ts",
    "madge": "madge --circular --extensions ts ./lib"
  },
  "type": "module",
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.6",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.12.13",
    "madge": "^7.0.0",
    "ts-morph": "^22.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "madge": {
    "detectiveOptions": {
      "es6": {
        "skipTypeImports": true
      },
      "ts": {
        "skipTypeImports": true
      }
    }
  }
}
